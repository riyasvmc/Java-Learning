package basics;

import java.util.regex.*;
import java.util.regex.Pattern;
import java.util.regex.Matcher;

/**
 * A Regular expression is a string of characters that describes a character sequence.
 *
 * Created by Riyas Valiyadan on 23 Oct 2020
 */

// (1/2) Don't have to declare functions inside class.

// (2/2) `main` does not require `args`, if your program doesn't require command-lines arguments.

public class Regex {
    public static void main(String[] args) {

    }
}
